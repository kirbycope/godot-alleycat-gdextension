/* PureAlleyCat is C and must be compiled as C: this unit holds the whole interpreter.
   The node in alley_cat.cpp includes the header for the declarations and links against this. */
#define ALLEYCAT_IMPLEMENTATION
#include "PureAlleyCat.h"
