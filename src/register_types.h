#pragma once

#include <godot_cpp/core/class_db.hpp>

using namespace godot;

void initialize_alley_cat_module(ModuleInitializationLevel p_level);
void uninitialize_alley_cat_module(ModuleInitializationLevel p_level);
